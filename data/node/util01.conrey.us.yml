---
classes:
  - 'prometheus'

prometheus::scrape_configs:
  - job_name: 'prometheus'
    scrape_interval: '10s'
    scrape_timeout: '10s'
    static_configs:
      - targets: ['localhost:9090','10.10.2.35:9100']
        labels:
          - alias: 'Prometheus'
